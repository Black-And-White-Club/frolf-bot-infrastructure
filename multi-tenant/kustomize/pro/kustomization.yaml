apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base

commonLabels:
  tier: pro

# Pro tier: Higher resource limits, potentially dedicated infrastructure
patchesStrategicMerge:
  - pro-resources.yaml
