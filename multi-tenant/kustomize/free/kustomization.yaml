apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base

commonLabels:
  tier: free

# Free tier: Lower resource limits, shared infrastructure
patchesStrategicMerge:
  - free-resources.yaml
